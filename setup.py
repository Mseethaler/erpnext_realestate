from setuptools import setup, find_packages

setup(
    name='erpnext_realestate',
    version='0.0.1',
    packages=find_packages(),
    zip_safe=False,
    include_package_data=True,
    install_requires=[]
)
